(define (problem sorting_problem)
(:domain sorting_domain)
(:objects can0 - Can middle_target - Target can0_init_target - Target can0_end_target - Target)
(:init  (CanAtTarget can0 can0_init_target))
(:goal (and (CanAtTarget can0 can0_end_target)))

)